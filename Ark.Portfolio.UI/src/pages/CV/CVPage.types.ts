import { CvDto, SkillDto, ExperienceDto, EducationDto } from '@ark/portfolio-share';

export interface CVPageProps { }

// Re-export shared types for local convenience if needed
export type { CvDto, SkillDto, ExperienceDto, EducationDto };

