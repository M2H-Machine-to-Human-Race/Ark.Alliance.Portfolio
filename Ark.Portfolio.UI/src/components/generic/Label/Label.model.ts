import { BaseComponentModel } from '../../base/BaseComponent.model';

export class LabelViewModel extends BaseComponentModel {
    // Label is mostly stateless / presentational, but we keep the pattern
    constructor() {
        super();
    }
}

